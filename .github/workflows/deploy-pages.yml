name: Publicar GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Configurar GitHub Pages
        id: pages
        uses: actions/configure-pages@v5

      - name: Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.3'
          bundler-cache: true

      - name: Build Jekyll para Pages
        env:
          JEKYLL_ENV: production
          PAGES_BASE_PATH: ${{ steps.pages.outputs.base_path }}
          PAGES_ORIGIN: ${{ steps.pages.outputs.origin }}
        run: bundle exec jekyll build --trace --baseurl "$PAGES_BASE_PATH" --url "$PAGES_ORIGIN"

      - name: Validar caminhos da publicação
        env:
          PAGES_BASE_PATH: ${{ steps.pages.outputs.base_path }}
        shell: bash
        run: |
          set -euo pipefail
          test -f _site/index.html
          test -f _site/assets/css/style.css
          test -f _site/assets/css/content.css
          test -f _site/assets/js/site.js
          grep -F "href=\"${PAGES_BASE_PATH}/assets/css/style.css\"" _site/index.html
          grep -F "src=\"${PAGES_BASE_PATH}/assets/js/site.js\"" _site/index.html

      - name: Enviar artefato do site
        uses: actions/upload-pages-artifact@v4
        with:
          path: ./_site

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Publicar no GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
